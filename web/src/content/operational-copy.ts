export const operationalCopy = {
  providerDisabled:
    'Paid AI is disabled. Current agent cost caps are zero; retained historical usage remains visible below.',
  signInTrust: 'Secure workspace access',
  signInDirect:
    'Continue with your authorised workspace identity. Provider credentials and tokens are not stored in browser storage.',
  mfaNotConfirmed: 'MFA not confirmed',
  sandboxMailbox: 'Sandbox mailbox',
  inventoryValidation:
    'Rule-based validation separates safe normalization from material review.',
} as const
