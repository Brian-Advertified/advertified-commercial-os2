# Advertified web application

This is the React 19.2.0/TypeScript/Vite surface for Advertified Commercial OS.

The current page is a Gate 0 development foundation only. It is not the authenticated product shell and it exposes no product capability.

Use the repository root `README.md` for setup, run, and verification commands. Contributor rules are in `AGENTS.md`.
