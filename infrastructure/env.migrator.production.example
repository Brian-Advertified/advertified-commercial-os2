# Advertified production one-shot database migrator environment.
# Supply the real value through the deployment secret/configuration store.
# This file is intentionally separate from the API environment so the API login
# never needs membership in the advertified_migrator database role.
ASPNETCORE_ENVIRONMENT=Production
ADVERTIFIED_MIGRATION_CONNECTION_STRING=REPLACE_WITH_MIGRATION_ONLY_DATABASE_CONNECTION
