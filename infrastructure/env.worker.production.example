# Advertified production worker-only environment.
# The worker also consumes the common/API production environment for canonical business commands.
# This file carries only the direct scheduler login used for SET ROLE advertified_worker.
ConnectionStrings__WorkerSchedulerDatabase=REPLACE_WITH_WORKER_DATABASE_CONNECTION
