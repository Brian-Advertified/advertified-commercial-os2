# Gates 4–13 specification record

**Status:** SUPERSEDED PLACEHOLDER  
**Controlling sources:** `docs/spec/README.md` and `docs/IMPLEMENTATION_PLAN.md`

The former file said Gates 4–13 were missing because the old plan stopped at a continuation marker. The complete owner-supplied v1.1 specification is now present in the repository and the current plan indexes each gate.

Do not implement a gate from this record. Use:

1. the exact normative sections listed by the current implementation plan;
2. Section 31 traceability/adversarial fixtures;
3. a newly approved gate work packet;
4. executable contracts/tests;
5. a named owner verdict.

Rapid OOH is a specialised planning path: approved Brief → geography/routes/POIs → verified inventory → deterministic eligibility → supplier confirmation → recalculation → human selection → approved plan → proposal. Generic proposal infrastructure may be reused, but proposal cannot precede plan approval.
